
const add_routes = (values) => { return { type: 'ADD_ROUTES', values }}
const add_redirect = values => { return { type: 'ADD_REDIRECT', values };}


export default {
  add_routes,
  add_redirect,
};
